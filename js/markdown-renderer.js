import { Utils } from './utils.js';
export const MarkdownRenderer=(()=>{const inline=raw=>{let t=Utils.escapeHtml(raw);t=t.replace(/`([^`]+)`/g,'<code>$1</code>').replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>').replace(/__([^_]+)__/g,'<strong>$1</strong>').replace(/\*([^*]+)\*/g,'<em>$1</em>').replace(/\[([^\]]+)\]\((https?:\/\/[^\s)]+)\)/g,'<a href="$2" target="_blank" rel="noopener noreferrer">$1</a>');return t;};
const table=rows=>{const p=rows.map(r=>r.trim().replace(/^\||\|$/g,'').split('|').map(c=>inline(c.trim())));return `<table><thead><tr>${(p[0]||[]).map(c=>`<th>${c}</th>`).join('')}</tr></thead><tbody>${p.slice(2).map(r=>`<tr>${r.map(c=>`<td>${c}</td>`).join('')}</tr>`).join('')}</tbody></table>`;};
const render=(md='')=>{const lines=String(md).replace(/\r\n/g,'\n').split('\n'),html=[];let i=0,code=false,buf=[],list=null;const close=()=>{if(list)html.push(`</${list}>`);list=null;};while(i<lines.length){const line=lines[i];if(line.trim().startsWith('```')){close();if(!code){code=true;buf=[];}else{html.push(`<pre><code>${Utils.escapeHtml(buf.join('\n'))}</code></pre>`);code=false;}i++;continue;}if(code){buf.push(line);i++;continue;}if(/^\s*\|.*\|\s*$/.test(line)&&i+1<lines.length&&/^\s*\|?\s*:?-{3,}/.test(lines[i+1])){close();const rows=[line,lines[i+1]];i+=2;while(i<lines.length&&/^\s*\|.*\|\s*$/.test(lines[i]))rows.push(lines[i++]);html.push(table(rows));continue;}const h=line.match(/^(#{1,4})\s+(.+)$/);if(h){close();html.push(`<h${h[1].length}>${inline(h[2])}</h${h[1].length}>`);i++;continue;}const u=line.match(/^\s*[-*+]\s+(.+)$/),o=line.match(/^\s*\d+\.\s+(.+)$/);if(u||o){const wanted=u?'ul':'ol';if(list!==wanted){close();html.push(`<${wanted}>`);list=wanted;}html.push(`<li>${inline((u||o)[1])}</li>`);i++;continue;}if(!line.trim()){close();i++;continue;}if(/^---+$/.test(line.trim())){close();html.push('<hr>');i++;continue;}close();const p=[line.trim()];i++;while(i<lines.length&&lines[i].trim()&&!/^(#{1,4})\s+/.test(lines[i])&&!/^\s*[-*+]\s+/.test(lines[i])&&!/^\s*\d+\.\s+/.test(lines[i])&&!lines[i].trim().startsWith('```'))p.push(lines[i++].trim());html.push(`<p>${inline(p.join(' '))}</p>`);}close();if(code)html.push(`<pre><code>${Utils.escapeHtml(buf.join('\n'))}</code></pre>`);return html.join('');};return{render};})();
